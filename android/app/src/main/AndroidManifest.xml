<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!--
      The restoration before/after is SHARED, never written to the photo library:
      the composite lives in the app cache and goes straight to the share sheet
      via react-native-share (which supplies its own FileProvider). That is why
      there are no storage permissions here — nothing of ours leaves the sandbox
      except through a share the user initiates.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />

    <!-- FCM pushes and the site-arrival notification. Runtime-requested from
         API 33; both surfaces degrade to silence if the user declines. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      LOCK-SCREEN TRANSPORT FOR THE NARRATION.

      NOT for background audio, which already works without any of this: a
      105 s clip was measured playing to completion with the screen locked and
      no service of any kind, because <Video playInBackground/> keeps ExoPlayer
      alive on its own. What was missing was only the CONTROL - a visitor
      walking a building with the phone in a pocket had no way to pause without
      unlocking, finding the app and finding the button.

      MEDIA_PLAYBACK is the narrow type, and it is the honest one: the only
      foreground service this app runs is react-native-video's, it only starts
      while a clip is actually playing, and it stops with the clip. Declaring
      the generic FOREGROUND_SERVICE alongside it is required - the typed
      permission is additional to it, not a replacement.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      
      <!--
        The same Google Maps API key authorises ARCore Geospatial requests, but
        the "ARCore API" must be explicitly enabled for this key in Google Cloud
        Console (APIs & Services → Library → "ARCore API"). Without it
        Earth.getEarthState() returns ERROR_APK_VERSION_TOO_OLD or
        ERROR_INTERNAL when first used at a heritage site.
      -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyAtDAq0FthJL3yC49neCn2jkzHNZHUnFFw"/>
      <meta-data
        android:name="com.google.ar.core"
        android:value="optional"/>
      <!--
        FCM notification defaults: without these, OS-rendered pushes fall back
        to the full-colour launcher icon, which Android draws as a blank
        white/grey square in the status bar. The small icon must be a
        white-on-transparent silhouette (ic_stat_epocheye, all densities).
      -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/ic_stat_epocheye"/>
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_color"
        android:resource="@color/notification_color"
        tools:replace="android:resource"/>
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="epocheye-default"
        tools:replace="android:value"/>
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Deep links: epocheye://site/<slug> (shared links bounce here from the website). -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="epocheye" />
        </intent-filter>
      </activity>

      <!--
        react-native-video's own MediaSessionService, declared HERE because the
        library ships an empty manifest and leaves the decision to the app -
        which is right, since it is the app that has to answer for a foreground
        service in review.

        It is started only by <Video showNotificationControls/>, and only two
        screens set that: the audio guide and the magic window, the two
        long-form narration surfaces. Everything else that plays audio
        (museum-mode narration, the journey step, a figure speaking) leaves it
        off, so a two-second line never puts a media notification on the lock
        screen.

        exported="false" because nothing outside the app should bind it; the
        intent-filter is what media3 routes hardware media buttons and
        notification actions through, so it stays even though our own binding
        is direct.
      -->
      <service
        android:name="com.brentvatne.exoplayer.VideoPlaybackService"
        android:exported="false"
        android:foregroundServiceType="mediaPlayback">
        <intent-filter>
          <action android:name="androidx.media3.session.MediaSessionService" />
        </intent-filter>
      </service>

    </application>
</manifest>
